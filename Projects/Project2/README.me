#include<map>

#include<utility>
= make.pair<Type1, Type2>  <string, node>(word, n)


Value Type
class Node {
public:
        Node() 
        {
              frequency = 1;
        }

        int frequency;
        vector<int> appears_on_lines;
};


Can use find() to serach through a map.

auto it = m.find(input)

if(it == m.end()){
    not found
}